// OPERADORES DE COMPARACION
/**
 * Igualdad -> ==
 * Igualdad extricta -> === / Evalua tambien el tipo
 * Desigualdad -> !=
 * Desigualdad extricta -> !== / Evalua tambien el tipo
 * -- TAMBIEN VALE PARA LOS STRING --
 * Mayor que -> >
 * Menor que -> <
 * Mayor o igual a -> >=
 * Menor o igual a -> <=
 */

// BUSCAR TABLAS UNICOD
let randomWord = 'alumno';

if (randomWord > 'hambre') {
  console.log('va despues');
} else if (randomWord < 'hambre') {
  console.log('va antes');
}

// OPERADORES LOGICOS
/**
 * AND -> &&
 * OR -> ||
 * NOT -> !
 */
