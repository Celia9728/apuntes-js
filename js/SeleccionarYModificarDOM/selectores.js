//  Seleccionar por id
let unico = document.getElementById('unico');

// Seleccionar por una etiqueta determinada
let etiqueta = document.getElementsByTagName('p');

// Seleccionar los elementos hijos de una clase
let hijosClase = document.getElementsByClass('clase');

// Seleccionar el primer elementos que encuentre del selector
let primerElemento = document.querySelector('elemento');

// Seleccionar varios elementos
let variosElementos = document.querySelectorAll('li');

// Seleccionar la etiqueta HTML
let html = document.documentElement;
