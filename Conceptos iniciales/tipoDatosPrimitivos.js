// STRING
/**
 * Representar texto
 * Se usan comillas dobre y simple.
 * Pueden contener cualquier tipo de texto.
 */
let cadena = '';
let cadena2 = 'Hola';
let cadena3 = '8';

// DATO NUMERICO
/**
 * Representan un numero
 * Cumplen el estandar IEEE 754 de 64 bits.
 * Almacena valores como minimo de y como maximo
 * Los numeros que excedan ese limite se convierten a Infinity
 * NaN es un numero especial cuando no se puede expresar con un numero
 */
let price = 185;

// BOOLEAN
/**
 * Se representa con 2 valores: true o false
 * Es usado en condicionales, en bucles y como retorno de una función.
 */
let esVerdad = true;

// UNDEFINED
/**
 * Valor por defecto que asigna JS a variables
 * que se han declaro pero no tienen valor asignado.
 */
let ourvalue;

// NULL
/**
 * Valor referenciado.
 * Es un valor que usamos de forma intencionada
 * para indicar que una variable tiene un valor
 * existente y que no existe siquiera su refencia en memoria.
 */
let ourvalue2 = null;

console.log(typeof ourvalue2); // Con el typeof nos dice el tipo de la variable
